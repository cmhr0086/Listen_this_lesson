# 更新日志

## 1.0.1 — 2026-09-03

### 异步 STT 与后台可靠性

- 将同步 `/v1/audio/transcriptions` 迁移为 `POST /transcribe` 提交音频，再通过 `GET /jobs/{job_id}` 查询结果。
- 待识别 WAV 使用本地持久队列和 WorkManager 处理，App 进入后台或进程重建后仍可恢复任务。
- 持久关联 `segmentId` 与 `jobId`；已有 `jobId` 时只查询状态，避免重复提交。
- 支持 429、503、`Retry-After`、服务端任务过期和提交状态未知等恢复路径。
- 增加 OkHttp 请求生命周期记录，不保存 API Key、Authorization Header、ASR Context 或响应正文。

> **服务端兼容性：** 1.0.1 需要支持上述异步任务接口的新服务端。仅提供 1.0.0 同步转写接口的旧服务端不能直接供本版本使用。

### ASR 诊断

- 开发者模式新增独立“ASR 诊断”页面，区分捕获、客户端排队、提交、服务端排队、模型处理、完成、失败和丢弃。
- 分别显示音频长度、客户端排队、上传、等待响应、服务端等待和端到端总耗时。
- 主诊断页仅显示当前课堂记录最近 15 条，完整诊断历史进入二级页面查看。
- `/health` 改为手动刷新，采集与 VAD 信息移动到诊断页顶部。
- 只有“嗯、啊、哦”等语气词的识别结果会被直接丢弃，不再写入课堂转写。

### AI 会话与内容处理

- 底栏新增全局“AI 会话”，汇总课程 AI 结果、课堂对话和通用对话。
- AI 聊天支持流式正文、思考内容展示与持久化。
- Markdown 增加标题、表格、任务列表、删除线、HTML 和代码块支持。
- 支持通过系统文件选择器添加图片以及 TXT、MD、CSV、JSON 文本附件。
- ASR 纠错改为结构化逐段结果，可应用纠正版、查看原文并恢复；原始 ASR 文本永久保留。
- 识别片段和 AI 内容列表支持长按后滑动范围多选，并保留批量导出和删除能力。

### 界面与交互

- AI Composer 改为悬浮式大圆角输入栏，修复输入法间距、底栏联动和附件菜单焦点问题。
- 恢复全局红色停止监听 FAB，切换到其他页面后仍可停止当前监听。
- AI 回复使用完整内容宽度，复制操作改为气泡右下角的小图标。
- 缩小应用图标主体并合拢书页，改善自适应图标显示。
- Room 数据库升级至 v8，并保留从旧版本逐级迁移的数据。

## 1.0.0 — 2026-09-02

Listen This Lesson 的首个正式版本。

### 主要功能

- 使用 AudioRecord 持续采集 16 kHz、单声道、PCM 16-bit 课堂音频。
- 使用 sherpa-onnx / Silero VAD 在设备端自然分段，ASR 请求期间继续监听。
- 通过 FIFO 队列调用远程 Qwen3-ASR，并按课程、课堂记录和识别片段保存结果。
- 支持课程级 ASR Context，以及关闭、自动和始终使用三种 Prompt 策略。
- 支持 OpenAI-compatible 与 DeepSeek AI 服务，可执行总结、笔记整理、ASR 纠错、快速回答和课堂对话。
- 支持 AI Markdown、流式回复、照片补充、片段多选、TXT 导出和前台监听通知。
- 支持运行时调整并持久化 VAD 参数、预设、AI 提示词和生成参数。

### 修复与体验

- 修复 ASR Prompt 相关页面因 Compose 编译与运行依赖不一致导致的闪退。
- ASR、DeepSeek 思考和推理强度选项统一为可横向滑动布局。
- AI 回复与固定结果卡片使用完整内容宽度。
- 新增适配 Android Adaptive Icon 和主题图标的“书本＋声波”应用图标。

### 平台说明

- 最低系统版本 Android 8.0（API 26）。
- 当前安装包仅支持 `arm64-v8a`。
- Android 端的 sherpa-onnx 只用于 VAD，所有语音识别仍由远程 Qwen3-ASR 完成。
